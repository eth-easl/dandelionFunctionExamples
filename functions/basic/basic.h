void noOperation();

int returnSeven();

int returnNumber(int number);

int addOne(int number);

// function for prototyping that allready contains all the code that should be
// added by dandelion for wrapping the function
void preWrapped(void);
